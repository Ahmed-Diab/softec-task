import { HoverCellDirective } from './hover-cell.directive';

describe('HoverCellDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverCellDirective();
    expect(directive).toBeTruthy();
  });
});
